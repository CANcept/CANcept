#pragma once

#include <QString>

namespace Sending::Style {
/**
 * @brief Common reusable styles across all pages.
 */
namespace Common {
/**
 * @brief Returns a stylesheet for a vertical scrollbar.
 * @return QString containing QSS for a vertical scrollbar.
 */
QString verticalScrollBar();
}  // namespace Common
}  // namespace Sending::Style