#pragma once

namespace FaultInjector {
/**
 * @struct ImmediateStrategy
 * @brief This means the fault happens immediatly after the trigger.
 */
struct ImmediateStrategy {
};

}  // namespace FaultInjector
